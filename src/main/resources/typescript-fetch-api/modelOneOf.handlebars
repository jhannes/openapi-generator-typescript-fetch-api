{{~#if description}}
/**
 * {{{description}}}
 */
{{~/if}}
export type {{classname}} ={{#if discriminator}}{{#discriminator}}
{{~#mappedModels}}
    { {{discriminator.propertyName}}: "{{mappingName}}" } & {{modelName}}{{#unless @last}} |{{/unless}}{{/mappedModels}}
{{~^mappedModels}}{{#oneOf}}{{{.}}}{{#unless @last}} | {{/unless}}{{/oneOf}}{{/mappedModels}}{{/discriminator}}{{else}} {{dataType}}{{/if}};

{{~#if discriminator}}
{{#discriminator}}
export const {{classname}}Descriminators = [
{{~#mappedModels}}
    "{{mappingName}}",
{{~/mappedModels}}
];
{{/discriminator}}
export type {{classname}}Descriminator = typeof {{classname}}Descriminators[number];
{{~/if}}
